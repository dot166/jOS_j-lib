<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:name=".app.RestorableSettingsApplication"
        android:backupAgent=".app.SettingsBackupHelper">
        <!-- Test Activity -->
        <activity
            android:name=".LIBTestActivity"
            android:icon="@android:drawable/sym_def_app_icon"
            android:label="@string/libplaceholder"
            android:theme="@style/Theme.Material3Expressive.DayNight.NoActionBar"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleInstance"
            android:exported="true" />
    </application>

</manifest>
